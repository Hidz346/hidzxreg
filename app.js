(() => {const $=s=>document.querySelector(s);const sens=$('#sens'),sensOut=$('#sensOut'),sensResult=$('#sensResult'),dpi=$('#dpi'),scale=$('#scale'),scaleOut=$('#scaleOut'),edpi=$('#edpi');function updateSensitivity(){const v=Number(sens.value);sensOut.value=v;sensResult.textContent=Math.max(1,Math.round(v*.85))+'–'+Math.min(200,Math.round(v*1.15))}function updateDpi(){const d=Math.max(0,Number(dpi.value)||0),s=Number(scale.value);scaleOut.value=s+'%';edpi.textContent=Math.round(d*s/100)}sens.addEventListener('input',updateSensitivity);dpi.addEventListener('input',updateDpi);scale.addEventListener('input',updateDpi);const profiles={low:['Performance profile','Low graphics · High frame rate · Minimal effects'],mid:['Balanced profile','Medium graphics · High frame rate · Standard effects'],high:['Quality profile','High graphics · High frame rate · Effects tuned to device']};$('#profileBtn').addEventListener('click',()=>{const p=profiles[$('#device').value];$('#profile').innerHTML='<strong>'+p[0]+'</strong><span>'+p[1]+'</span>'});document.querySelectorAll('.preset').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.preset').forEach(x=>x.classList.remove('active'));b.classList.add('active')}));$('#themeBtn').addEventListener('click',()=>{document.documentElement.style.setProperty('--bg','#f5f6f8');document.documentElement.style.setProperty('--panel','#fff');document.documentElement.style.setProperty('--panel2','#eef1f5');document.documentElement.style.setProperty('--text','#10131a');document.documentElement.style.setProperty('--muted','#5f6878');document.documentElement.style.setProperty('--line','#d8dde5');$('#themeBtn').textContent='Dark';$('#themeBtn').onclick=()=>location.reload()});updateSensitivity();updateDpi()})();